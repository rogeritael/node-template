<!--
  adicionar projeto ao github

  //gerar SSH e adicionar ao github para poder clonar projeto pelo usuário da AWS
  ssh-keygen
  cat ~/.ssh/id_rsa.pub

  mkdir app
  cd app
  git clone url_do_projeto
  cd nome_do_projeto
  npm install

  cd dist
  node server.js

  //alterar arquivo .env
  cp .env.example .env
  vim .env



:wq!
-->
